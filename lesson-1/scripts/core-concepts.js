// Variables and data types

// Use 'let' for variables that might change.
let name = "John"; // string

// Use 'const' for values that you don't intend to change, but remember,
// for objects and arrays declared with 'const', their properties or elements can still be modified.
const fullName = "John Doe"; // string

// Primitive Data Types: string, number, boolean, undefined, null, symbol
/* let age = 25; // number
let isStudent = true; // boolean
let hobbies = ["reading", "coding", "gaming"]; // array

// Reference Data Types
let person = {
  // object
  name: "Jane",
  age: 30,
  isStudent: false,
}; */

/* // Operators
let x = 5;
let y = 3;

// Arithmetic Operators
let sum = x + y; // addition
let difference = x - y; // subtraction
let product = x * y; // multiplication
let quotient = x / y; // division
let remainder = x % y; // modulus

console.log("Sum: ", sum);
console.log("Difference: ", difference);
console.log("Product: ", product);
console.log("Quotient: ", quotient);
console.log("Remainder: ", remainder); */

/* // Comparison Operators
console.log("Equal to (==): ", x == y);
console.log("Strictly equal to (===): ", x === y);
console.log("Not equal to (!=): ", x != y);
console.log("Strictly not equal to (!==): ", x !== y);
console.log("Greater than (>): ", x > y);
console.log("Less than (<): ", x < y);
console.log("Greater than or equal to (>=): ", x >= y);
console.log("Less than or equal to (<=): ", x <= y);

// Logical Operators
let a = true;
let b = false;

console.log("Logical OR (||): ", a || b);
console.log("Logical AND (&&): ", a && b);
console.log("Logical NOT (!): ", !a); */

/* // Control Flow - if statement
if (age >= 18) {
  console.log("You are an adult.");
} else {
  console.log("You are a minor.");
} */

/* // Control Flow - loops
// for loop
for (let i = 0; i < hobbies.length; i++) {
  console.log(hobbies[i], "has this position in the array:", i);
} */

/* // while loop
let counter = 0;
while (counter < 5) {
  console.log("Counter: ", counter);
  counter++;
} */
